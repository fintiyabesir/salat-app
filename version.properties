# Single source of truth for the marketing version.
# Android reads it through the root build script; the Apple build reads it in CI.
# The build number is NOT stored here — it is derived from the commit count.
versionName=0.1.0
